const auth = require('./auth')
const orders = require('./orders')
const payments = require('./payments')
const seller = require('./payments')
const user = require('./payments')

module.exports = { auth, orders, payments, seller, user }
